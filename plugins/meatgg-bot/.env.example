# Installed to the bot's state directory (~/bots/meatgg/.state/.env) by `setup`; never read from the repo.

# ApiKey from /admin/api-keys, owned by an admin holding VIEW_TICKETS.
MEATGG_API_URL=https://meat.gg
MEATGG_API_KEY=

LOG_LEVEL=info
NODE_ENV=production
