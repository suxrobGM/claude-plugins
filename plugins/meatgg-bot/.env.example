# Installed to ~/.claude/channels/meatgg/.env by `setup`; the bot never reads it from the repo.

# ApiKey from /admin/api-keys, owned by an admin holding VIEW_TICKETS.
MEATGG_API_URL=https://meat.gg/api
MEATGG_API_KEY=

LOG_LEVEL=info
NODE_ENV=production
